rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ปิดฐานข้อมูลไว้ก่อนจนกว่าจะเพิ่ม Firebase Authentication
      // และกำหนดสิทธิ์แยกระหว่างครูกับนักเรียน
      allow read, write: if false;
    }
  }
}
